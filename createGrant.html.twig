{% extends 'base.html.twig' %}

{% block body %}

    {{ form_start(form) }}

    {{ form_row(form.name) }}
    {{ form_row(form.label) }}
    {{ form_row(form.description) }}
    {{ form_row(form.requiredPermissions) }}
    {{ form_row(form.application) }}

    <button type="submit"> Create </button>

    {{ form_end(form) }}

{% endblock %}